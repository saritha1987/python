def is_harshad(number):
    return number % sum(int(digit) for digit in str(number)) == 0
number = int(input("Enter a number: "))
print(f"{number} is {'a Harshad number' if is_harshad(number) else 'not a Harshad number'}.")


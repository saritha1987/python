def is_perfect(num):
    return num > 0 and num == sum(i for i in range(1, num) if num % i == 0)
number = int(input("Enter a number: "))
print(f"{number} is a perfect number." if is_perfect(number) else f"{number} is not a perfect number.")
